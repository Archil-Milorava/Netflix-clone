import { DesctiptionPipe } from './desctiption.pipe';

describe('DesctiptionPipe', () => {
  it('create an instance', () => {
    const pipe = new DesctiptionPipe();
    expect(pipe).toBeTruthy();
  });
});
